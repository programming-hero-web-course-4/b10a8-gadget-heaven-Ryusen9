import { useRoutes } from "react-router-dom"

const BodySection = () => {
  const element = useRoutes([
    {
      path: "/",
      element: 
    }
  ])
  return element;
}

export default BodySection